/* ============================================
   ESTILOS PERSONALIZADOS PARA LOGIN
   Página profesional y moderna para jazzmin-login-page
   VERSIÓN CON COLORES SOBRIOS
   ============================================ */

/* Background con gradiente profesional - Gris azulado oscuro */
body.jazzmin-login-page {
  background: linear-gradient(135deg, #2c3e50 0%, #34495e 50%, #2c3e50 100%);
  background-attachment: fixed;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

/* Efecto de partículas/patrón de fondo */
body.jazzmin-login-page::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-image: radial-gradient(
      circle at 20% 50%,
      rgba(255, 255, 255, 0.05) 0%,
      transparent 50%
    ),
    radial-gradient(
      circle at 80% 80%,
      rgba(255, 255, 255, 0.05) 0%,
      transparent 50%
    ),
    radial-gradient(
      circle at 40% 20%,
      rgba(255, 255, 255, 0.03) 0%,
      transparent 50%
    );
  animation: floatBackground 20s ease-in-out infinite;
}

@keyframes floatBackground {
  0%,
  100% {
    transform: translateY(0) scale(1);
  }
  50% {
    transform: translateY(-20px) scale(1.05);
  }
}

/* Contenedor principal del login */
body.jazzmin-login-page .login-box,
body.jazzmin-login-page .card {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 20px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3),
    0 0 0 1px rgba(255, 255, 255, 0.1) inset;
  border: none;
  overflow: hidden;
  animation: slideUp 0.6s ease-out;
  max-width: 450px;
  width: 100%;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Header del card de login - Gris oscuro profesional */
body.jazzmin-login-page .card-header,
body.jazzmin-login-page .login-logo {
  background: linear-gradient(135deg, #34495e 0%, #2c3e50 100%);
  color: white;
  border: none;
  padding: 2rem 1.5rem;
  text-align: center;
  position: relative;
}

body.jazzmin-login-page .card-header::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(90deg, #3498db, #2ecc71, #3498db);
  background-size: 200% 100%;
  animation: accentShift 4s linear infinite;
}

@keyframes accentShift {
  0% {
    background-position: 0% 50%;
  }
  100% {
    background-position: 200% 50%;
  }
}

/* Logo del login */
body.jazzmin-login-page .login-logo a,
body.jazzmin-login-page .brand-link {
  color: white !important;
  font-weight: 700;
  font-size: 1.8rem;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
  letter-spacing: 0.5px;
}

body.jazzmin-login-page .login-logo img,
body.jazzmin-login-page .brand-image {
  filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
  transition: transform 0.3s ease;
}

body.jazzmin-login-page .login-logo img:hover,
body.jazzmin-login-page .brand-image:hover {
  transform: scale(1.05) rotate(2deg);
}

/* Texto de bienvenida */
body.jazzmin-login-page .login-box-msg,
body.jazzmin-login-page .card-header h3,
body.jazzmin-login-page .card-header p {
  color: white;
  font-weight: 500;
  font-size: 1.1rem;
  margin: 0.5rem 0 0;
  opacity: 0.95;
}

/* Body del card */
body.jazzmin-login-page .card-body,
body.jazzmin-login-page .login-card-body {
  padding: 2.5rem 2rem;
  background: white;
}

/* Inputs del formulario */
body.jazzmin-login-page .form-control,
body.jazzmin-login-page input[type="text"],
body.jazzmin-login-page input[type="password"],
body.jazzmin-login-page input[type="email"] {
  border: 2px solid #d1d5db;
  border-radius: 10px;
  padding: 0.85rem 1rem 0.85rem 3rem;
  font-size: 1rem;
  transition: all 0.3s ease;
  background: #f9fafb;
  height: auto;
}

body.jazzmin-login-page .form-control:focus,
body.jazzmin-login-page input:focus {
  border-color: #3498db;
  box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
  background: white;
  outline: none;
}

/* Grupo de input con icono */
body.jazzmin-login-page .input-group {
  margin-bottom: 1.5rem;
  position: relative;
}

body.jazzmin-login-page .input-group-text {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 3rem;
  background: transparent;
  border: none;
  z-index: 10;
  color: #5a6c7d;
  font-size: 1.2rem;
  pointer-events: none;
}

body.jazzmin-login-page .input-group .fas,
body.jazzmin-login-page .input-group .fa {
  color: #5a6c7d;
  transition: all 0.3s ease;
}

body.jazzmin-login-page .input-group:focus-within .fas,
body.jazzmin-login-page .input-group:focus-within .fa {
  color: #3498db;
  transform: scale(1.1);
}

/* Botón de login */
body.jazzmin-login-page .btn-primary,
body.jazzmin-login-page button[type="submit"] {
  background: linear-gradient(135deg, #34495e 0%, #2c3e50 100%);
  border: none;
  border-radius: 10px;
  padding: 0.9rem 2rem;
  font-weight: 600;
  font-size: 1.05rem;
  letter-spacing: 0.5px;
  width: 100%;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(44, 62, 80, 0.3);
  text-transform: uppercase;
  position: relative;
  overflow: hidden;
}

body.jazzmin-login-page .btn-primary::before {
  content: "";
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.2),
    transparent
  );
  transition: left 0.5s ease;
}

body.jazzmin-login-page .btn-primary:hover::before {
  left: 100%;
}

body.jazzmin-login-page .btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(44, 62, 80, 0.5);
  background: linear-gradient(135deg, #3c4f64 0%, #34495e 100%);
}

body.jazzmin-login-page .btn-primary:active {
  transform: translateY(0);
  box-shadow: 0 2px 10px rgba(44, 62, 80, 0.3);
}

/* Checkbox "Recordarme" */
body.jazzmin-login-page .icheck-primary,
body.jazzmin-login-page .form-check {
  margin: 1.5rem 0;
}

body.jazzmin-login-page .icheck-primary label,
body.jazzmin-login-page .form-check-label {
  color: #4b5563;
  font-weight: 500;
  cursor: pointer;
  user-select: none;
}

body.jazzmin-login-page input[type="checkbox"] {
  width: 18px;
  height: 18px;
  cursor: pointer;
  accent-color: #3498db;
}

/* Links (Olvidé mi contraseña, etc) */
body.jazzmin-login-page a {
  color: #3498db;
  text-decoration: none;
  font-weight: 500;
  transition: all 0.3s ease;
  position: relative;
}

body.jazzmin-login-page a::after {
  content: "";
  position: absolute;
  bottom: -2px;
  left: 0;
  width: 0;
  height: 2px;
  background: #2c3e50;
  transition: width 0.3s ease;
}

body.jazzmin-login-page a:hover {
  color: #2c3e50;
}

body.jazzmin-login-page a:hover::after {
  width: 100%;
}

/* Mensajes de error */
body.jazzmin-login-page .alert,
body.jazzmin-login-page .errorlist {
  border-radius: 12px;
  border: none;
  padding: 1rem 1.25rem;
  margin-bottom: 1.5rem;
  animation: shake 0.5s ease;
}

body.jazzmin-login-page .alert-danger,
body.jazzmin-login-page .errorlist {
  background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
  color: #991b1b;
  box-shadow: 0 4px 12px rgba(220, 38, 38, 0.2);
}

@keyframes shake {
  0%,
  100% {
    transform: translateX(0);
  }
  10%,
  30%,
  50%,
  70%,
  90% {
    transform: translateX(-5px);
  }
  20%,
  40%,
  60%,
  80% {
    transform: translateX(5px);
  }
}

body.jazzmin-login-page .alert-success {
  background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
  color: #065f46;
  box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
}

body.jazzmin-login-page .alert-info {
  background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
  color: #1e40af;
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.2);
}

/* Footer del card */
body.jazzmin-login-page .card-footer {
  background: #f8fafc;
  border: none;
  padding: 1.5rem 2rem;
  text-align: center;
  color: #6b7280;
  font-size: 0.9rem;
}

/* Copyright */
body.jazzmin-login-page .text-center.text-muted {
  color: rgba(255, 255, 255, 0.8) !important;
  margin-top: 2rem;
  font-size: 0.9rem;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

/* Mejoras para campos con errores */
body.jazzmin-login-page .form-control.is-invalid,
body.jazzmin-login-page input.is-invalid {
  border-color: #ef4444;
  background: #fef2f2;
}

body.jazzmin-login-page .form-control.is-invalid:focus {
  box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.1);
}

/* Spinner de carga */
body.jazzmin-login-page .spinner-border {
  width: 1.2rem;
  height: 1.2rem;
  border-width: 0.2em;
  margin-right: 0.5rem;
}

/* Responsive design */
@media (max-width: 576px) {
  body.jazzmin-login-page .login-box,
  body.jazzmin-login-page .card {
    margin: 1rem;
    max-width: calc(100% - 2rem);
  }

  body.jazzmin-login-page .card-body,
  body.jazzmin-login-page .login-card-body {
    padding: 2rem 1.5rem;
  }

  body.jazzmin-login-page .card-header,
  body.jazzmin-login-page .login-logo {
    padding: 1.5rem 1rem;
  }

  body.jazzmin-login-page .login-logo a,
  body.jazzmin-login-page .brand-link {
    font-size: 1.5rem;
  }
}

/* Animación de entrada para elementos */
body.jazzmin-login-page .form-group,
body.jazzmin-login-page .input-group {
  animation: fadeInUp 0.6s ease-out backwards;
}

body.jazzmin-login-page .form-group:nth-child(1) {
  animation-delay: 0.1s;
}
body.jazzmin-login-page .form-group:nth-child(2) {
  animation-delay: 0.2s;
}
body.jazzmin-login-page .form-group:nth-child(3) {
  animation-delay: 0.3s;
}
body.jazzmin-login-page .btn-primary {
  animation-delay: 0.4s;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Efecto hover en el card completo */
body.jazzmin-login-page .login-box:hover,
body.jazzmin-login-page .card:hover {
  box-shadow: 0 25px 70px rgba(0, 0, 0, 0.35),
    0 0 0 1px rgba(255, 255, 255, 0.15) inset;
  transform: translateY(-5px);
  transition: all 0.3s ease;
}

/* Brillo sutil en el fondo */
body.jazzmin-login-page::after {
  content: "";
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(
    circle,
    rgba(255, 255, 255, 0.1) 0%,
    transparent 70%
  );
  animation: rotate 30s linear infinite;
  pointer-events: none;
}

@keyframes rotate {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

/* Asegurar que el contenido esté por encima de los efectos */
body.jazzmin-login-page .login-box,
body.jazzmin-login-page .card,
body.jazzmin-login-page .text-center {
  position: relative;
  z-index: 1;
}

/* ============================================
   BOTÓN PARA MOSTRAR/OCULTAR CONTRASEÑA
   ============================================ */

/* Botón de toggle de contraseña */
.password-toggle-btn {
  position: absolute;
  right: 12px;
  top: 50%;
  transform: translateY(-50%);
  background: transparent;
  border: none;
  color: #5a6c7d;
  font-size: 1.1rem;
  cursor: pointer;
  padding: 8px;
  border-radius: 6px;
  transition: all 0.3s ease;
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
}

.password-toggle-btn:hover {
  background: rgba(52, 152, 219, 0.1);
  color: #3498db;
  transform: translateY(-50%) scale(1.1);
}

.password-toggle-btn:active {
  transform: translateY(-50%) scale(0.95);
}

.password-toggle-btn.active {
  color: #3498db;
  background: rgba(52, 152, 219, 0.15);
}

.password-toggle-btn:focus {
  outline: none;
  box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.2);
}

/* Animación del icono */
.password-toggle-btn i {
  transition: all 0.3s ease;
}

.password-toggle-btn:hover i {
  transform: scale(1.15);
}

/* Ajuste para el input con botón de toggle */
body.jazzmin-login-page input[type="password"],
body.jazzmin-login-page input[type="text"][name="password"] {
  padding-right: 50px !important;
}

/* Tooltip para el botón */
.password-toggle-btn::after {
  content: attr(title);
  position: absolute;
  bottom: -35px;
  left: 50%;
  transform: translateX(-50%) scale(0);
  background: #2c3e50;
  color: white;
  padding: 6px 12px;
  border-radius: 6px;
  font-size: 0.75rem;
  white-space: nowrap;
  pointer-events: none;
  opacity: 0;
  transition: all 0.3s ease;
  z-index: 1000;
}

.password-toggle-btn:hover::after {
  transform: translateX(-50%) scale(1);
  opacity: 1;
}

/* Flecha del tooltip */
.password-toggle-btn::before {
  content: "";
  position: absolute;
  bottom: -8px;
  left: 50%;
  transform: translateX(-50%) scale(0);
  width: 0;
  height: 0;
  border-left: 6px solid transparent;
  border-right: 6px solid transparent;
  border-bottom: 6px solid #2c3e50;
  opacity: 0;
  transition: all 0.3s ease;
  pointer-events: none;
  z-index: 1001;
}

.password-toggle-btn:hover::before {
  transform: translateX(-50%) scale(1);
  opacity: 1;
}

/* Responsive para móviles */
@media (max-width: 576px) {
  .password-toggle-btn {
    right: 8px;
    width: 32px;
    height: 32px;
    font-size: 1rem;
  }

  body.jazzmin-login-page input[type="password"],
  body.jazzmin-login-page input[type="text"][name="password"] {
    padding-right: 45px !important;
  }

  /* Ocultar tooltip en móviles (usar title nativo) */
  .password-toggle-btn::after,
  .password-toggle-btn::before {
    display: none;
  }
}
